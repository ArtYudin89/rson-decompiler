# Python
__pycache__/
*.pyc

# Результаты декомпиляции и временные артефакты прогона
/decompile_result/
*_check.scr
*.dialogs.txt

# Локальные эталоны/скрины/рабочие заметки — публичный репо, копирайтный
# контент модов не выкладываем (файлы остаются на диске, но не в git)
/reference/

# Сторонние утилиты (не распространяем чужие бинари) — положить локально
/RScript_*/
/BlockParEditor_*/
/ResEditor_*/
/ResEditor_*.zip

# Игровые файлы (копирайт) — не коммитим
/Space Rangers HD*/
